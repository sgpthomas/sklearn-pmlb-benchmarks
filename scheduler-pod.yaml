apiVersion: v1
kind: Pod
metadata:
  name: d3m-pmlb-scheduler-pod
spec:
  restartPolicy: Never
  containers:
  - name: d3m-sklearn-pmlb-benchmarks
    image: registry.datadrivendiscovery.org/d3m/sklearn-pmlb-benchmarks:D3M_sklearn-pmlb-benchmarks
    env:
    - name: D3MINPUTDIR
      value: "/input"
    - name: D3MOUTPUTDIR
      value: "/output"
    - name: D3MTYPE
      value: "scheduler"
    - name: D3MCPU
      value: "1"
    - name: D3MRAM
      value: "512Mi"
    - name: D3MTIMEOUT
      value: "10"
    volumeMounts:
      - name: output-data
        mountPath: /output
        readOnly: false
    resources:
      requests:
        memory: 512Mi
        cpu: 1
  imagePullSecrets:
  - name: regcred
  volumes:
  - name: output-data
    persistentVolumeClaim:
      claimName: eval-output-pv-claim
